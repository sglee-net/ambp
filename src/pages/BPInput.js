import React, {Component} from 'react';
import BPSet from '../molecules/BPSet';

class BPInput extends Component {
    render() {
        return (
            <React.Fragment>
                <BPSet/>
            </React.Fragment>
        );
    }
}

export default BPInput;